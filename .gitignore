node_modules/
out/
dist/
coverage/
.vite/
*.log
*.tsbuildinfo

# Local environment files never belong in version control. Jupiter keeps
# credentials in OS-backed secure storage, never in .env files.
.env
.env.*
!.env.example

# Test and verification evidence produced locally (CI uploads its own copy).
test-results/
