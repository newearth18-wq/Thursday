# Security

## Reporting a vulnerability

Please report security problems privately to the repository owner through
GitHub's private vulnerability reporting, not in a public issue.

## Principles

These come from the Jupiter Global Contract and apply to every SET.

1. **Deny by default.** Nothing is granted unless a central policy grants it.
2. **The renderer is untrusted UI.** It has no Node.js, filesystem, shell,
   credential or unrestricted IPC access.
3. **Every trust boundary is validated** with typed schemas, in both directions.
4. **External content is data, never instructions** — web pages, documents,
   plugin output and model output cannot grant permissions or change policy.
5. **Secrets live only in OS-backed secure storage.** Never in source, plaintext
   files, databases, logs, the renderer, crash reports or error messages.
6. **Privileged actions go through one Permission Engine** with exact targets,
   risk levels and audit records.
7. **Third-party code never runs in the Electron main process.**
8. **Failures are reported truthfully**, sanitized, with a way to recover.

## What is enforced today (SET 0–7)

| Control                                                                                                                                                                                                                                                                                                       | Where                                                                                                    | Verified by                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `contextIsolation`, `sandbox`, no `nodeIntegration` (also in workers and subframes), no `<webview>`                                                                                                                                                                                                           | `apps/desktop/src/main/window.ts`                                                                        | E2E reads the live web preferences                                                               |
| `app.enableSandbox()` for every renderer (skipped only with an explicit `--no-sandbox`, shown in Diagnostics)                                                                                                                                                                                                 | `apps/desktop/src/main/index.ts`                                                                         | E2E + Diagnostics                                                                                |
| No `remote` module: removed from Electron, `@electron/remote` not a dependency; no `require`, `process` or `ipcRenderer` in the page                                                                                                                                                                          | `package-lock.json`, preload                                                                             | E2E checks the page globals                                                                      |
| Preload exposes seven fixed functions, frozen; no generic `invoke`, no `ipcRenderer`                                                                                                                                                                                                                          | `apps/desktop/src/preload/index.ts`                                                                      | E2E checks the exact bridge keys and that it cannot be modified                                  |
| Exactly six invoke channels are registered; any other channel is unreachable                                                                                                                                                                                                                                  | `apps/desktop/src/main/gateway.ts`                                                                       | unit test + E2E reads Electron's handler table                                                   |
| Sender check on every channel: the Jupiter window, top frame, `jupiter://app` origin — otherwise denied and audited as `unverified`                                                                                                                                                                           | `apps/desktop/src/main/gateway.ts`                                                                       | E2E: a second renderer with the same preload is denied on all channels                           |
| Actor assigned by the gateway, never read from the message; capability authorization deny-by-default per actor type                                                                                                                                                                                           | `gateway.ts`, `packages/core/src/dispatch/dispatcher.ts`                                                 | unit tests + E2E (`runtime.report-host-status` denied to the UI)                                 |
| Strict request envelopes and per-capability payload and output schemas; 256 KiB size limit; functions cannot cross the bridge                                                                                                                                                                                 | `packages/contracts`, gateway, dispatcher                                                                | unit tests + E2E (AT2)                                                                           |
| Privileged host functions only through the Core dispatcher (`provider: host`); the one host-served action (`retry-service`) is audited                                                                                                                                                                        | `apps/desktop/src/main/host-capabilities.ts`, ADR 0002                                                   | unit tests + E2E audit check                                                                     |
| Every refusal and every state-changing or host capability call is written to the append-only audit log                                                                                                                                                                                                        | `packages/database` (`audit_log` triggers)                                                               | integration + E2E                                                                                |
| Interface served from `jupiter://app` only: GET, no traversal (plain or encoded), no symlinks, other hosts refused; CSP sent as a header                                                                                                                                                                      | `apps/desktop/src/main/app-protocol.ts`                                                                  | unit tests + E2E probes the live handler                                                         |
| Strict CSP: no inline or evaluated script, no remote content, `connect-src 'none'`, no frames or workers                                                                                                                                                                                                      | `apps/desktop/src/shared/csp.ts`                                                                         | build-output test + E2E inline-script violation                                                  |
| The page cannot read local files: `fetch`, XHR and `window.open` of `file://` fail; no capability reads files or credentials                                                                                                                                                                                  | CSP, protocol handler, capability catalogue                                                              | E2E (AT10)                                                                                       |
| Nothing returned to the renderer contains a secret from the environment                                                                                                                                                                                                                                       | contracts carry no secret fields                                                                         | E2E launches with credential-shaped variables and scans every reply                              |
| Navigation, redirects, new windows, webviews, downloads and permission requests blocked                                                                                                                                                                                                                       | `apps/desktop/src/main/security.ts`                                                                      | E2E navigation and window.open tests                                                             |
| Jupiter Core isolated in a utility process with no Electron window or shell access; crashes contained and reported                                                                                                                                                                                            | `apps/desktop/src/core`, `core-process.ts`                                                               | build-output test + unit tests + E2E (AT9)                                                       |
| Desktop notifications only as a dispatcher capability (`host.notifications.show`): schema-limited title and body, audited, at most six a minute; a click only focuses the Jupiter window (SET 2)                                                                                                              | `host-capabilities.ts`, `packages/core/src/kernel/capabilities.ts`                                       | unit tests                                                                                       |
| Preferences are Core settings: typed and validated on write by the dispatcher, audited, and validated again on read — an invalid stored value falls back to the default and is never rewritten (SET 2)                                                                                                        | `packages/contracts/src/settings.ts`, `core-kernel.ts`                                                   | unit tests + E2E (invalid stored value, database down)                                           |
| Window state file validated on read (an invalid file is ignored with a logged warning and replaced by the next save), written atomically with owner-only permissions, holds only bounds and the last screen; restored bounds are fitted to the current displays (SET 2)                                       | `apps/desktop/src/main/window-state.ts`                                                                  | unit tests + E2E (AT2)                                                                           |
| Dev server URL honoured only for unpackaged development on loopback                                                                                                                                                                                                                                           | `packages/core/src/environment.ts`                                                                       | unit tests                                                                                       |
| DevTools only in development                                                                                                                                                                                                                                                                                  | environment profiles                                                                                     | packaged-app test                                                                                |
| Renderer cannot import Node.js or Electron                                                                                                                                                                                                                                                                    | `eslint.config.js` (`no-restricted-imports`)                                                             | lint + build-output test                                                                         |
| Log redaction (credential key names + credential formats) in both processes, bounded sizes, owner-only log files                                                                                                                                                                                              | `packages/security`, `packages/core`                                                                     | unit, integration and E2E tests                                                                  |
| No hardcoded credentials in sources, build output or the packaged app                                                                                                                                                                                                                                         | `scripts/check-secrets.mjs`, `scripts/validate-package.mjs`                                              | integration test + package validation                                                            |
| API keys only in OS-backed secure storage: Electron `safeStorage` (DPAPI on Windows, Keychain on macOS, Secret Service on Linux); one ciphertext file per key, mode 0600 in a 0700 folder; storing is refused when only Chromium's unprotected fallback (`basic_text`) exists (SET 3)                         | `apps/desktop/src/main/credential-vault.ts`                                                              | unit tests + E2E (AT2: ciphertext file, permissions, keyring marker, key readable after restart) |
| Keys never leave the host and Core: the renderer sends a key once and gets back only a fingerprint; Core reads it through host operations that are not capabilities and are refused for any actor but Core; the database holds only a credential id and fingerprint (SET 3)                                   | `host-capabilities.ts`, `packages/contracts/src/host-operations.ts`, `packages/core/src/ai/providers.ts` | contract test (no output schema can carry a secret) + unit tests + E2E (AT8)                     |
| Provider error text is sanitized before it is stored, logged or shown: the key, echoes of it and any other credential format are removed (SET 3)                                                                                                                                                              | `packages/core/src/ai/sanitize.ts`                                                                       | unit tests + adapter tests + E2E (AT3: a provider that echoes the wrong key)                     |
| One network path for AI: adapters get only Core's guarded transport (ESLint forbids `fetch` and Node network modules in `packages/providers`); redirects refused; no key over plain `http` to an address that is not on this computer (SET 3)                                                                 | `packages/core/src/ai/transport.ts`, `eslint.config.js`                                                  | unit tests + E2E (unencrypted cloud address)                                                     |
| Local only: locality comes from the address (only loopback is this device); a cloud address is refused before any connection is opened, and the refusal is recorded (SET 3)                                                                                                                                   | `transport.ts`, `router.ts`                                                                              | unit + adapter tests + E2E (AT7: zero connections to the cloud server)                           |
| Model output is plain text: rendered as text, never as markup; hidden reasoning is dropped in the adapters and never stored or shown; tool calls are displayed, never run (no tools until SET 6) (SET 3)                                                                                                      | `packages/providers`, `ChatView.tsx`                                                                     | adapter tests + E2E                                                                              |
| Chat message text never goes into the event log (events carry metadata only); streamed text is transient (SET 3)                                                                                                                                                                                              | `packages/contracts/src/events.ts`                                                                       | contract tests                                                                                   |
| Mission status changes only through the state machine in Core; a refused change is stored with its reason, published, and audited with its error code (SET 4)                                                                                                                                                 | `packages/contracts/src/missions.ts`, `packages/core/src/missions/manager.ts`, dispatcher audit          | contract + in-process + E2E (AT4)                                                                |
| Mission history cannot be rewritten or erased: transitions, errors, verification results and artifacts are append-only (triggers); Retry adds an attempt instead of changing one (SET 4)                                                                                                                      | `packages/database/src/schema.ts` (migration 4)                                                          | database integration tests                                                                       |
| Cancelling a Mission aborts its active step's signal, which closes the provider request (SET 4)                                                                                                                                                                                                               | `MissionManager`, `completeText`                                                                         | in-process + E2E (AT6)                                                                           |
| Model output reaches the Workflow Engine only after a strict schema (unknown fields such as reasoning are refused) and the plan validator; rejected plans are stored with their reasons and nothing of them runs; no model reasoning is stored or shown (SET 5)                                               | `packages/contracts/src/plans.ts`, `packages/core/src/workflow/validate.ts`                              | unit + in-process + E2E (AT2, AT10)                                                              |
| A plan that names a capability Jupiter does not know cannot run; known capabilities are asked for when a step uses them (SET 7). Step types are a fixed catalogue, and identity checkpoints are unavailable (SET 5)                                                                                           | `packages/core/src/workflow/{catalogue,validate}.ts`                                                     | unit + in-process (AT10)                                                                         |
| A step's output is written once, in the transaction that completes it; a stored output is never produced again, so retries and recovery cannot duplicate side effects. Plan revisions, rejections and step attempts are append-only (SET 5)                                                                   | `MissionManager.runStep`, migration 5                                                                    | in-process (AT7, AT9) + database tests                                                           |
| Cancel aborts every running step, and a step past its time limit is aborted, closing its provider request (SET 5)                                                                                                                                                                                             | `MissionManager`                                                                                         | in-process + E2E (AT5, AT6)                                                                      |
| Every Skill invocation runs in its own worker thread, in a `vm` context without `require`, `process`, timers or environment variables, with a memory limit; timeout and cancel terminate the thread. Not a hardened boundary for hostile code: third-party code waits for the plugin runtime (SET 15) (SET 6) | `packages/core/src/node/worker-sandbox.ts`                                                               | sandbox tests + in-process + E2E (AT4, AT5, AT8)                                                 |
| A Skill reaches nothing but the resources it declared; each resource fixes its capability and exact target, and every use is checked by the Permission Engine. The caller of `skills.invoke` cannot pass permissions (SET 6, SET 7)                                                                           | `packages/core/src/skills/registry.ts`                                                                   | in-process (AT7; SET 7 AT1)                                                                      |
| One Permission Engine decides every action with an effect: deny by default; a grant must match capability, requester, exact target (or `*` prefix), Mission, session and expiry; unknown capabilities are always denied (SET 7)                                                                               | `packages/core/src/permissions/engine.ts`, `PERMISSION_CATALOGUE`                                        | in-process (SET 7 AT1, AT9) + E2E                                                                |
| ALLOW_ONCE is used up in the transaction that allows the action; ALLOW_SESSION ends with the Core process; CRITICAL actions (and HIGH actions of automations) accept only a fresh ALLOW_ONCE and never offer Always allow (SET 7)                                                                             | `PermissionEngine.check`, `offeredDecisions`                                                             | in-process (AT4, AT5, AT6) + E2E                                                                 |
| Only the person (`user-interface`) can answer requests or revoke grants: enforced by the dispatcher policy and again by the engine; plugins, automations, runtimes and content (model output, Skill input, pages, documents) have no path to change policy (SET 7)                                            | `kernel/capabilities.ts`, `PermissionEngine.decide/revoke`                                               | in-process (AT2, AT8)                                                                            |
| Every evaluation, request, answer and grant change is written to an append-only audit trail (triggers refuse UPDATE and DELETE); targets, reasons and details are redacted, and a target containing a secret is stored redacted and never matched by a grant (SET 7)                                          | migration 7, `PermissionEngine.audit`                                                                    | database tests + in-process (AT10) + E2E                                                         |
| Grants are ended (used, expired, revoked), never deleted; Jupiter's default grants are visible and revocable, and a revoked default is never recreated (SET 7)                                                                                                                                                | `PermissionEngine.start`, Settings › Permissions                                                         | in-process (AT7) + E2E                                                                           |
| Skill metadata, input and output are validated against strict schemas; invalid output fails the execution (SET 6)                                                                                                                                                                                             | `packages/contracts/src/skills.ts`, `skills/schema-check.ts`                                             | unit + in-process (AT1, AT2, AT9)                                                                |
| Skill execution history keeps only the shape and size of input and output, never content; error text is redacted (SET 6)                                                                                                                                                                                      | `summarize`, migration 6                                                                                 | in-process (AT3) + database tests + E2E                                                          |

## Not yet in place (by design, later SETs)

Capabilities with effects outside Jupiter (files, browser, computer, email: later SETs — the Permission Engine already governs them), identity (SET 14),
plugin isolation (SET 15), signed updates (SET 21), code signing and installer
hardening (SET 20), Electron fuses and full threat model (SET 23). The legacy
Thursday Browser in `legacy/` does **not** meet these principles (for example it
can store API keys unencrypted when no OS keychain exists, and it pins Electron
38 with known advisories); it is kept only as a reference and is not part of
Jupiter.
